# 启动器主线官网与根域名入口

状态：已上线并验收。替代官网首页的工作台优先叙事，公开广场数据合同沿用现有真实数据方案。

1. 官网正式入口使用 `https://prompt.likh.cn/`，旧 `/preview/` 跳转根路径并保留页内导航；精确静态路由保留 `/v1/`、OAuth 和其他既有 API 路由。
2. 首屏围绕独立启动器与可配置全局快捷键，展示真实搜索、变量填写界面，说明回车复制、快捷创建、AI 优化与广场搜索。下载为主要操作，网页广场为次要操作。
3. 首页改为清晰的大字层级、克制的青绿品牌色和真实启动器主视觉；补充键盘流程、提示词库、Skills 与网页/桌面能力边界，更新至 beta.15 下载入口。
4. 默认中文 README 和六份译本均将启动器移到介绍、截图和功能顺序的首位，加入官网链接；保留实际截图说明。
5. 检查桌面/窄屏导航、截图切换、下载链接、真实广场读取及控制台；备份服务器路由与静态目录，Compose 部署、Nginx 检查后上线并验证根地址、旧链接与 API 健康。

- Given 打开官网或任意语言 README When 阅读首屏 Then 明确产品核心是全局快捷启动器，展示搜索与填写复制，不承诺自动粘贴。
- Given 首页的操作预览 When 切换搜索和变量填写 Then 显示实际应用截图，图文及选中状态同步，键盘可操作。
- Given 根域名与旧预览链接 When 访问 Then 根域名提供官网，旧链接重定向且保留工作台页内导航。
- Given 桌面或 390px 窄屏 When 浏览与导航 Then 无横向页面溢出，下载、真实广场与主要内容可达。
- Given 修改网站路由 When 验证 Then `/v1/health`、广场 API 和管理端仍正常，不变更数据库。

## 验收结果

- [正式官网](https://prompt.likh.cn/) 已上线，以真实启动器搜索与变量填写截图为首屏；默认快捷键、复制流程、创建、AI 优化、广场搜索、本机库、Skills 和 MCP 的用途及平台边界均已说明。
- [GitHub 默认首页](https://github.com/sutao2/CueTuck) 及六份译本均以启动器为第一介绍与截图，127 个相对链接有效；仓库 About 的官网地址与简介同步更新。
- 6 项公开数据模块测试、文档检查与 diff 检查通过。官网、下载、提示词和 Skills 四页 × 1440/960/390 三种宽度无横向溢出。
- 浏览器验证截图切换与键盘选择、beta.15 双平台下载链接、真实广场 24 条分页与列表切换。公网复验旧 `/preview/#app` 跳至 `/#app` 且保留片段导航，手机布局与首页返回通过，无控制台错误或警告。
- 15 个线上静态文件 SHA-256 全部匹配白名单构建产物；排除了本机 Vite 缓存。根页面 canonical、公开索引及 robots/sitemap 使用正式域名。
- Compose 目录 `/opt/cuetuck-website-preview/releases/20260915-launcher-1` healthy；Nginx 配置校验通过。旧版本和路由备份见部署说明，API 的 Postgres/Redis/MinIO 均健康，管理端正常。
- Nginx reload 的首个检查短暂命中旧 worker 的 404，脚本有界重试后根页面与公网复验通过。没有修改业务数据库、账号配置或桌面安装包。
