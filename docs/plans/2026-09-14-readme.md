# 多语言项目首页

状态：已验收。

1. 默认 README 使用简体中文；增加 English、日本語、한국어、Español、Français、Deutsch 六份对应译本，共享截图和语言导航。
2. 根据当前代码与实际发行状态介绍广场、本地模板、变量、启动器、AI、Skills、MCP、安装和开发；区分预览版、桌面与浏览器能力，不宣称 Linux 或所有宿主均通过验证。
3. 用当前应用代码的隔离浏览器预览截图：广场使用公开内容，本地与启动器使用自编示例。截图不修改界面、不带登录身份、密钥或本机私人路径；说明浏览器截图与桌面能力的边界。
4. 检查所有相对链接、语言互链与图片显示，运行文档校验，提交并推送默认分支。

- Given 打开 GitHub 首页 When 阅读 README Then 默认中文且能跳到各语言版本。
- Given 任意语言版本 When 点击截图、下载和文档链接 Then 指向有效文件或实际发行入口。
- Given 首页介绍某项功能 When 对照现行实现 Then 文案不将计划、模拟数据或未验证平台写成已交付事实。

## 验收记录

- 中文首页与六份译本使用相同功能结构、安装边界和四张实际 UI 截图。
- `scripts/capture-readme.mjs` 使用独立浏览器会话与只读开发代理获取公开广场；本地和启动器使用进程内示例库。`--launcher-only` 可单独重拍启动器。
- 四张 PNG 已逐一查看，无登录账号或密钥。截图保留真实浏览器模式状态，不伪装原生能力。
- 7 份 README 的 134 个相对目标、42 个语言切换链接有效；GitHub GFM 渲染中文首页得到 5 个图片元素（含图标）、2 个表格与 1 个命令代码块。
- `./scripts/docs-check` 通过，`git diff --check` 通过。本次仅调整项目介绍与截图脚本，不修改应用功能。
