# 剩余优化闭环

状态：设计完成，实施中。范围为本轮列出的五项优化及部署、双平台发行。

## 已确定方案与验收

1. 推荐：仅推荐排序应用本机「不感兴趣」（最多 200 条，可恢复），换批排除最近已加载 48 条；分页固定本轮排除集。候选耗尽提供重新浏览入口，不影响分类总数。Given 已屏蔽或上一批内容，When 换批，Then 不返回排除 ID；最新/收藏不受影响。
2. 搜索：补充常见用途的中英词汇匹配与英文拼写近似匹配，字面匹配优先，明确不等同任意语义翻译。结果安全高亮实际命中的文本。Given 拼写误差或中英用途词，When 搜索，Then 找到对应内容，特殊字符不作为代码执行。
3. 图片：有界短期缓存已校验图片，失败重试复用成功项，只重试失败项；不完整下载仍不得落库和计数。Given 一张失败，When 重试，Then 已成功项不重新请求，缓存过期重新校验。
4. 发布：提交前检查标题、正文、分类、模型和实际选择的公开附件，确认后提交；返回修改保留表单。Given 编辑中的投稿，When 预览，Then 不发请求，确认后才投稿。
5. 状态：复用一致的加载/空白/错误呈现，错误明确可重试，空白可清筛选，旧请求不可覆盖新查询。
6. 全端测试、真实浏览器检查、按现行部署方案备份部署、构建 Mac/Windows 并验证发行附件。

## 进度

- [x] 推荐反馈与跨批去重：6 项 UI、3 项真实 PG（含 2 万候选）、7 项网页数据回归通过
- [x] 搜索与高亮：40 组共享用途词、英文单字符容错；真实 PG、SQLite 和安全高亮通过
- [x] 图片缓存与单项重试：9 项前端、5 项原生通过；两项显式公网下载测试未运行
- [x] 发布预览：冻结提交快照、附件选择和失败重试；浏览器 1440/820/600 验收
- [x] 状态统一：提示词、社区、本机/GitHub Skills 的加载、空白和错误统一
- [ ] 回归、部署与发行

## 本机回归

640 项客户端测试、网页端和管理端测试/构建、后端和原生全量、MCP 均通过。浏览器核心与管理端通过；管理端使用独立 1452 端口避免占用用户 1432。真实浏览器搜索高亮、推荐屏蔽、发布前不发送请求及窄窗检查通过；修正预览自动垂直居中的过大留白。截图在 output/playwright/completion-*，不提交。

## 交付计划

版本 0.1.0-beta.17。沿用原更新签名、应用 identifier 和数据目录，纳入 beta.16 后的直接使用、排序、布局及本次五项补齐。先备份数据库/部署配置，再独立构建新 API 和管理端镜像并切换；官网保留上一份静态目录。Mac arm64 本机构建、Windows x64 固定同一提交 CI 构建，附件签名与校验和验证后公开预览发行，最后切换官网入口。
