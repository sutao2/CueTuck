# 启动器完整交互审查

状态：核心修复及当前环境可执行验收完成；跨应用成功路径、物理全局快捷键及其他平台仍待实机复验，不视为全平台完成。用户指定启动器为核心流程，保留独立窗口、偏上位置和输入清空时的锚点；不改主窗口架构。

## 实施与验收

1. 搜索与填写：先补失败回归，再修复明确的键盘路由、首次/返回焦点、输入法、多行、变量名安全、搜索状态与高亮滚动。验证 0/1/多变量、重复/空/特殊名称、未填、长内容和超过一屏结果。
2. 使用与生命周期：验证复制成功/拒绝/重试/连续 Enter、记录失败、关闭偏好、失焦与重新唤起；以 native shown/hidden 事件同步状态，操作进行中不让失焦重置待完成文本。
3. 原生桥接：复用旧启动器的稳定焦点等待；macOS 原生剪贴板检查退出状态；读取选中文本明确针对原应用及当前变量；最近粘贴不复用过时目标。验证权限失败、目标退出、焦点未就绪的降级，禁止向未经核实的应用发送按键。
4. 实际验收：Playwright 独立 launcher 页面按真实键盘运行隔离样例，检查 680 × 80/500/520、浅深色、长内容滚动；Rust 单元测试、release 搜索基准及桌面回归/构建。原生跨应用测试只使用专门测试输入区，不覆盖用户提示词；不可验证的系统权限与平台明确列出。

每个独立切片测试、审查后提交。只修改本轮启动器和共享渲染/粘贴所必需的部分，不以测试数量代替实测证据。

## 验收记录

- 核心修复完成：新增首轮 8 个用例时 7 个失败；最终扩展到 16 个交互回归全部通过。覆盖逐项回车、换行、输入法、重复按键、失败重试/保留、焦点、原生事件替身及当前变量读取。共享渲染修复继承属性误填；后续失败用例验证了 Vue 保留键冲突，启动器改用 Map 存储变量输入。
- 2026-09-07：桌面 36 文件 / 259 测试通过；Rust 60 通过、1 基准默认忽略；单独 release 基准 1 万条查询 2.14ms；前端生产构建及 Rust 构建通过。Rust 存在既有本地数据库未使用导出警告，未扩大范围清理。
- Playwright 已实测重复变量、多行填写、逐项 Enter、最后一项回车写入隔离剪贴板替身；最终文本与实时预览一致。20 条可见结果 End 导航滚动至最后一项且在可见范围。截图存于未跟踪 `output/playwright/launcher-*`。
- Playwright 技能用于真实浏览器键盘操作及截图检查，未把组件事件当成原生实测。浅/深色、680×80/500/520、12 变量逐项导航、长正文 656px 通栏预览、Tab/Shift+Tab、3 次搜索/填写/Esc 循环均通过；无页面横向溢出。浏览器唯一控制台错误为既有 favicon 404，没有本轮运行时异常。
- 原生使用隔离测试包 `PromptArk Audit`（`app.promptark.audit`）创建 1 条测试提示词；未修改用户的正式数据库。原生回车复制后在未保存的测试编辑区按 Cmd+V，核实中文、重复变量及换行均一致；测试草稿已关闭、测试包已退出。该包的 `.app` 成功生成并启动，但整条打包命令因缺少更新包签名私钥最终退出 1，不声明发布打包通过。

### 功能逐项结果

| 功能 | 证据与结论 |
|---|---|
| 本地搜索/空查询/异步逆序/无结果与失败 | 组件回归通过，无广场/管理 API 请求；基准满足预算 |
| 上下/PageUp/PageDown/Home/End/鼠标选择 | 键盘单测、短列表负下标回归通过；浏览器 End 滚动到第 20 项且可见 |
| 0/1/多/重复/空/特殊变量与字面值 | 渲染与交互回归通过；浏览器长正文/12 变量，原生重复变量与中文换行通过 |
| Enter/Ctrl/Cmd+Enter/Shift+Enter/Tab | 回归及浏览器真实键盘通过；原生逐项 Enter、最后一项复制通过 |
| 输入法确认/长按重复 | 组字与 keyCode 229/重复按键回归通过；真实中文输入法候选确认仍需人工复验 |
| 复制成功及失败重试 | 原生真实剪贴板通过；拒绝写入不记使用/可重试由替身回归验证 |
| 使用记录/最近文本/空复制 | 回归通过，记录失败不误报复制失败；原生主窗口最近计数在刷新后变化 |
| 自动关闭/保持打开 | 原生分别验证末项回车关闭、保持填写并显示已复制；窗口隐藏失败保留草稿有回归 |
| Esc/返回/再唤起/迟到焦点/失焦 | 浏览器循环、生命周期替身通过；原生 Esc 与再次空查询通过；自动化切换应用窗口导致失焦隐藏可见，纯键入再唤起的焦点时序未作通过保证，已补 focus 到达时恢复当前字段及回归 |
| 读取原选区到当前变量 | 当前变量/空选区/失败保留由替身验证；原生无辅助功能权限时错误准确、两项旧值不变 |
| 自动粘贴 | 原生权限拒绝后显示已复制及具体原因，填写保留；成功跨应用路径受权限阻挡，未执行 |
| 原目标退出/不稳定焦点 | 目标分类和连续稳定焦点/超时单测通过；未向未核实应用注入按键；外部应用退出场景未原生实测 |
| 全局唤起/新建/最近粘贴快捷键 | 注册及持久化回归通过；隔离包设置保存成功，但自动化合成全局组合未观察到响应，不据此声明物理快捷键通过 |
| 位置、拖动和跨平台 | 位置保持/缩放计算合同回归通过，保留原独立窗口实现；未对拖动、多显示器及 Windows/Linux 作原生实测；后两平台自动粘贴明确降级手动 |

### 后续验收入口

由用户自行决定是否为正式应用开启 macOS 辅助功能权限。授权后在专门的空白输入区复验：原应用选区填入第二变量、保持窗口偏好下跨应用粘贴、切换原应用后粘贴最近文本、目标退出时不误粘贴；再用物理键盘核验三组全局快捷键与中文输入法。未授权前不绕过系统权限，不宣称这些路径通过。
