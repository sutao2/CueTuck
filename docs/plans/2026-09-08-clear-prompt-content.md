# 清空当前本机内容

## 授权与范围

用户确认同时清空现有广场提示词/合集及本地提示词，包括自建内容。保留账号、认证配置、分类字典、软件设置和代码样本文件；只清理本机开发实例和正式桌面数据目录，不处理审计测试实例。

## 顺序

1. 核对本机 Docker、依赖表和桌面 SQLite：广场 50 条，本地 3 条可见及 8 条已删除，无合集、附件、云同步或投稿记录。
2. 备份 PostgreSQL public 和整个桌面 SQLite，校验可读取、计数与完整性；备份位于 output 下权限 700 的独立目录，不进入 Git。
3. 后端增加持久化 `square_seed_disabled=true` 运行标志，清空后即使重启也不重建演示内容。先测试空库、禁用及历史条目的判断。
4. 暂停桌面/后端写入，事务清空目标内容与最近复制缓存，同时写入禁用标志；核对其他表计数及设置保持。
5. 重启服务与桌面，确认列表为零、已知正文 404，本地完整性检查通过；记录备份位置供恢复。

## Given / When / Then

- GIVEN 用户已确认且备份通过，WHEN 清空本地和广场，THEN 当前提示词与关联图片引用清空，账号/设置保留。
- GIVEN 空广场且禁用种子标志为 true，WHEN 后端重启，THEN 不补回演示条目；没有该标志的新开发库仍保留既有初始化行为。
- GIVEN 清理过程中失败，WHEN 尚未提交，THEN 对应数据库事务回滚，保留备份可恢复。

## 验收

- 完整备份保留在本机 `output/content-backup-hADxik/`，包含权限 600 的 SQLite 与 PostgreSQL dump、校验和及恢复注意事项；备份不提交 Git。
- 事务删除广场 50 条（包括图片 URL 引用）及本地全部 11 条记录，清理最近复制缓存；无实际附件/对象存储记录，不删除外部站点图片。
- 账号 2 条及原有后端设置的内容指纹与清理前一致；桌面其余设置逐行比对和分类双向差集检查均保持，SQLite integrity_check 为 ok。
- 种子禁用回归与内容管理相关 5 项测试通过；后端构建通过，已用不重置 schema 的配置重启，桌面也重新打开。
- 重启后 `/v1/health` 三项正常、广场 API 返回 `items: []`，旧条目正文为 404，数据库广场记录仍为 0，确认未回填。
