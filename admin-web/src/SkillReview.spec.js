import {mount,flushPromises} from '@vue/test-utils';import {afterEach,it,expect,vi} from 'vitest';import SkillReview from './SkillReview.vue';import {resetAdminApi,setAdminApiTransport} from './adminApi.js';
let w;afterEach(()=>{w?.unmount();resetAdminApi();});
it('requires file review confirmation and sends the expected status',async()=>{const row={id:'one',title:'Skill',status:'pending',body:'<script>no()</script>',files:[{path:'SKILL.md',size:8}],publisher:{display_name:'作者'},history:[]};const t=vi.fn(async r=>r.method==='POST'?{status:'approved'}:r.riskPath==='skills/one'?row:{items:[row],total:1});setAdminApiTransport(t);w=mount(SkillReview);await flushPromises();const click=async text=>{await w.findAll('button').find(b=>b.text()===text).trigger('click');await flushPromises();};await click('查看完整文件');expect(w.find('script').exists()).toBe(false);expect(w.findAll('button').find(b=>b.text()==='审核通过').attributes('disabled')).toBeDefined();await w.get('input[type=checkbox]').setValue(true);await click('审核通过');expect(t).toHaveBeenCalledWith(expect.objectContaining({riskPath:'skills/one/review',method:'POST',config:{status:'approved',reason:'',expected_status:'pending'}}));expect(w.text()).toContain('审核结果已保存');});
