# 文档与代码对齐

状态：实施中。范围为本次核查确认的总状态、架构、API 合同与验证入口，不改变应用行为。

## 切片与验收

1. 更新总状态、架构和索引，引用已记录的发行及能力边界；保留历史计划作为历史证据。
   - Given 默认本地搜索及显式广场入口 When 阅读现行架构 Then 与启动器和 MCP 的实际分支一致。
   - Given beta.20 发行记录及停用 WorkLog 的决定 When 阅读总状态 Then 不再声称尚无公开下载或等待 WorkLog 批准。
2. 按实际处理器补全 Skills、翻译及模型发现的 14 个路径，保留公开/登录/管理权限区分。
   - Given 后端路由 When 对照两个 OpenAPI 文件 Then 路径与 HTTP 方法双向一致，路径参数名称差异不误报。
   - Given 后端新增路径/方法或文档残留旧操作 When 运行合同检查 Then 失败并列出差异；无法解析的路由不得静默跳过。
3. 将路由检查及其失败回归接入集中验证，说明结构检查与语义检查的边界。
   - Given CI 仅执行 cargo test When 阅读门禁 Then 明确覆盖率及 Testcontainers 尚未落实，不能宣称达标；保留 ADR 0008 的要求，不降低阈值。

每片完成运行对应检查并提交。不重新执行线上部署，不把历史发行记录当成本轮公网复验。全后端覆盖率测量、补测及 Testcontainers 迁移另行实施。

## 进度

- [x] 状态、架构与索引对齐：`python3 scripts/docs-check`（335 个 Markdown）与 `git diff --check` 通过；按现行能力规格及已提交发行记录核对。
- [ ] API 合同与真实路由对照
- [ ] 验证入口、失败回归与门禁现状说明
