# 功能复审与修复

| 字段 | 值 |
|---|---|
| 状态 | 现行，队首 |
| 关联 | [状态](status.md) · [分类规格](../specs/categories/spec.md) |

旧计划关闭不等于完整产品验收通过。按真实用户操作复查，每个切片通过回归后立即提交，未验收项不得标完成。

## 切片顺序

1. 本地分类：分类计数按内容、新建继承当前位置、保留未分类入口、筛选结果与标题一致。验证组件交互与 SQLite 筛选。
2. 广场分类：发布保存系统分类、列表过滤、下载保留分类及模型。验证 API → 桌面与 Web 下载，历史无分类条目仍可访问。
3. 同步完整性：自定义分类、合集与成员、模型、时间戳、删除状态的往返及冲突。验证独立设备、较新者胜、保留本地。
4. 工作台与数据操作：合集成员、复制失败、导入导出、设置刷新。逐个写失败场景再修复。
5. 其余模块复审：Web、管理台、认证、启动器、更新、账单；跑对应门禁，记录实际验证边界与剩余缺口。

## 当前验收

- [x] 切片 1：桌面 172 个测试通过，SQLite 未分类回归通过，docs-check 180 文件通过。
- [ ] 切片 2
- [ ] 切片 3
- [ ] 切片 4
- [ ] 切片 5

生产托管、商店账号、Windows/Linux 设备验证仍见 [deferred](deferred.md)。不修改真实用户提示词的分类或正文来掩盖产品问题。
