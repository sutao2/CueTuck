# Windows 一体式顶栏

状态：已实现，Windows 原生窗口按钮验收通过，已随 [beta.21 发布](2026-09-21-beta21-release.md)。按用户提供的 Codex Windows 截图优化主窗口顶部。

## 实施与验收

1. 仅 Windows 主窗口关闭重复的原生标题条，将窗口按钮与现有侧栏折叠、当前位置、全局搜索融合。保留 macOS 原生红绿灯、Linux 窗框和独立启动器。
2. 通过 Tauri 窗口接口最小化、最大化/还原和关闭；关闭仍经过既有关闭到托盘规则。窗口按钮独立于业务面板的 inert 状态，在设置、搜索、登录及确认界面仍可用，窗口拖动区域不覆盖按钮。
3. 补按钮动作、最大化状态监听及清理、窗口隔离和平台差异回归；验证浅深色、800px 窄窗及设置/搜索布局。运行桌面全量与构建，并验证 Windows 原生构建；未实际完成的系统交互明确记录。

- Given Windows 主窗口 When 启动 Then 顶部只有应用绘制的一体式栏，三个窗口按钮不被搜索或长标题挤出。
- Given 最大化/还原或系统窗口尺寸改变 When 顶栏刷新 Then 按钮图标与可访问标签反映实际状态。
- Given 设置、搜索或确认界面 When 点击窗口按钮 Then 窗口操作仍可执行，关闭沿用既有托盘策略。
- Given macOS、Linux 或独立启动器 When 使用新版 Then 保留原窗口装饰和快捷键行为。

不引入截图中的聊天功能或无对应业务的菜单。Windows 系统拖动、缩放、贴靠的实机结果与浏览器样式验证分开记录。

## 进度

- 新增窗口控制与宿主隔离回归 9 项，桌面全量 675、Web 33、管理后台 106 项及三个构建通过；API 合同、文档和工具检查通过。日志 `output/verification/run-jpP54e/`。
- macOS 原生 140 项通过，6 项既有外部测试忽略；使用已安装 Command Line Tools 执行，未更改 Xcode 许可设置。
- Windows CI 增加真实 UI Automation 按钮调用：先核对客户端顶部无额外原生标题条，再操作最大化、还原、最小化和关闭，验证原生窗口状态；保留已有安装/启动/卸载验收。此门禁不等于完成多显示器/DPI 和 Windows 11 贴靠浮层的手工验证。
- 隔离浏览器浅深色、800px 侧栏展开、搜索弹层和设置页验收通过：窗口按钮始终可命中，不被 inert 或业务弹层遮挡，搜索不与窗口控制重叠；截图 `output/playwright/windows-titlebar-*`。
- Windows 原生构建、135 项测试及实际窗口按钮检查已通过，发行及完整远程回归证据统一见上述 beta.21 记录。
