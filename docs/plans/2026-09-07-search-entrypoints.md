# 软件内搜索与启动器入口分离

状态：已实现，当前环境验收完成。

## 实施与验收

1. 顶栏与侧栏搜索定位当前空间已有搜索框，保留查询与筛选，不增加第二个表单；支持 Cmd+F / Ctrl+F，输入框与入口提示一致，弹窗和输入法组字不抢焦点。
2. 底栏明确标为「启动器」，保留原生独立窗口动作；快捷键标签读取本机保存值，设置完成启动器注册与持久化后立即通知工作台，失败不展示未保存草稿。
3. 先补入口、键盘、持久化回读、成功/失败保存的回归；跑全部前端测试、真实浏览器操作、桌面打包与文档检查，审查后提交。测试不修改正式用户快捷键。

## 验收记录

- 先确认新回归在旧实现失败，再完成修复。前端 38 文件 / 283 项测试通过；覆盖 Mac/Windows 键盘分支、两空间搜索聚焦、弹窗/组字保护、保存值回读、未保存/注册失败和其他设置写入失败时的标签一致性。
- Playwright 验证顶部与侧栏聚焦、选中已有查询、真实 Cmd+F、侧栏收起后广场搜索可输入；截图 `output/playwright/search-entrypoints.png` 为未跟踪验收产物。
- 原生隔离包 `app.promptark.audit` 首次显示已存的 ⌃⌥K；顶部点击后在本页搜索成功，设置录入并保存 ⌃⌥J 后底栏显示新值，顶部保持 ⌘F；底栏点击打开独立「提示方舟启动器」窗口。测试包已退出，正式用户快捷键未改。
- 正式 macOS `.app` 构建通过；本切片不修改全局注册机制，不声称 Windows 原生或物理全局热键额外实测。保留已有 Rust 未使用代码警告，不做无关清理。
