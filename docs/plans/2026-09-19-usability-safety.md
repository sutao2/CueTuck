# 使用体验与可恢复性

状态：已实施并完成本地验收，随后随 [beta.20 发布](2026-09-19-beta20-release.md)；未替换用户已安装应用。用户要求优化评审列出的体验问题，并保护既有功能。

## 边界

保留独立启动器、现有同步冲突策略、手动附件授权、收藏/下载分离及既有数据库字段。新增本机恢复能力不自动上传文件、不操作真实用户库；验证使用临时库与浏览器内存库。库内附件实际存于 SQLite，备份已包含这些字节；外链内容不作完整恢复承诺。

## 切片与验收

1. 快捷键与同步状态：显示启动注册失败及修复入口，保存成功清除错误；将自动同步队列准确命名，显示当前账号的队列与最近手动同步结果。Given 冲突/账号切换 When 启动或查看状态 Then 不伪装成功、不显示上一账号状态，原按钮仍可用。
2. 搜索与日常操作：全局搜索增加本机 Skills 和显式社区 Skill 范围，保持草稿离开保护；明确复制/填写、本地保存、收藏范围；空库提供可选示例；Skills 维护入口折叠。Given 用户搜索 Skill 或首次使用 When 选择结果或示例 Then 可完成原有流程，取消不写数据，搜索不安装文件。
3. 本地回收站：提示词/合集可恢复，删除后可撤销；保留成员分离合同，恢复不覆盖现有条目且修复失效分类/合集引用。Given 删除含附件条目 When 重启后恢复 Then 正文与文件完整，原搜索/使用/同步仍有效。
4. 备份说明与恢复预览：明确库内附件和外链边界，恢复前校验并显示内容数量；失败保留原库，恢复前保存当前库。Given 有附件的备份或损坏文件 When 预览/恢复 Then 文件完整或拒绝写入；预览不修改原文件。
5. 同步覆盖历史：在较新远端正文覆盖本机前保留本机版本，提供查看和恢复为独立副本；不改变较新者胜/保留本地与附件传输边界。Given 正文发生冲突 When 同步后恢复旧版本 Then 当前同步条目不被覆盖，旧版本可查看并复制为本地提示词。

每片补针对性失败/成功回归，更新对应规格和本记录后提交。最后执行前端全量测试与构建、原生测试、MCP/后端回归及浏览器流程，记录无法执行的外部验证。无需发布或替换已安装应用。

## 进度

- [x] 快捷键与同步状态：新增账号隔离状态与注册失败回归，原发送规则不变
- [x] 搜索与日常操作：149 项针对性测试通过（含草稿、复制失败、Skills 安装及新搜索范围）
- [x] 本地回收站：2 项 SQLite 文件恢复/成员保留与 30 项前端回归通过
- [x] 备份与恢复预览：5 项原生校验/回滚/WAL/附件测试、29 项前端回归通过
- [x] 同步覆盖历史：SQLite 事务回滚、保留本地、幂等重放及附件副本恢复通过；前端 35 项针对性回归通过（预览测试使用真实 toggle 事件）
- [x] 全量回归与浏览器检查；搜索详情使用单一可点击返回入口，底栏设置入口保留草稿离开保护

## 最终验收（2026-09-19）

- `node scripts/verify.mjs frontend`：桌面 666、Web 33、管理后台 106 项测试及三个构建通过，文档和工具脚本检查通过。日志：`output/verification/run-YaWJmU/`。
- 原生回归 140 项通过，6 项既有外部测试忽略；MCP 27 项通过。日志：`output/verification/run-nrFh8z/`、`run-zkdAp6/`。
- 后端在独立临时 PostgreSQL 中完成 182 项单元与 15 项集成测试，4 项既有测试忽略。日志：`output/verification/run-l9m8Uu/`。未连接生产数据库。
- 原桌面浏览器 smoke 通过，日志 `output/verification/run-p0IDAu/browser.log`；管理后台 smoke 使用空闲端口 1452 通过登录、列表返回、驳回、失败重试、退出和三种宽度检查，截图 `output/playwright/admin-1789799608165/`。
- 隔离内存库浏览器验收通过：示例保存、删除撤销、回收站恢复、同步旧版本预览与副本恢复、快捷键修复入口、Skills 搜索详情和返回、600 像素无横向溢出。截图与操作记录：`output/playwright/safety-*`。
- 实际浏览器发现并修正了 Skills 返回按钮空字符串导致误禁用的问题，新增可点击状态断言；最终前端全量与新增浏览器流程均重新通过。
- 本轮未执行 Windows 实机或安装包更新验证；上述既有忽略项未宣称通过。原有未跟踪文件保留，真实用户库和已安装应用未改动。
