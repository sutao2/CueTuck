server {
    listen 80;
    listen [::]:80;
    server_name prompt.likh.cn prompt-admin.likh.cn;
    location ^~ /.well-known/acme-challenge/ { root /www/wwwroot/promptark-acme; }
    location / { return 503; }
}
