# 分类新建与删除修复

状态：已实现，当前环境功能验收完成。

1. 新建改为可选择大分类的弹窗，选中小分类时默认其父类；全库也可直接新建。保留两级规则，同级同名与空白拒绝，提交防重复、失败保留表单。广场不展示本地分类管理动作，折叠按钮不再用减号伪装删除。
2. 自定义小分类提供删除及确认；系统分类不允许删。SQLite 事务与浏览器内存行为一致，分类软删除，所属提示词/合集转未分类，内容及合集成员关系不变，刷新选中项和数量。
3. 为分类加入删除时间兼容迁移及同步墓碑；远端删除落地也清空对应分类引用，避免同步或重启后复活。导出不包含已删除分类。
4. 补前端交互、内存数据、原生命令参数、Rust 分类与同步回归；跑测试、构建、隔离 UI 验收与文档检查后提交，不修改正式用户数据。

## 验收记录

- 前端 40 文件 / 290 项通过；Rust 63 项通过、1 项原有忽略。覆盖创建校验、IME、双提交、父分类选择、取消/失败删除、系统分类保护、保留正文与成员、数据库事务回滚及墓碑同步往返。
- Playwright 从全库点击新建，确认名称聚焦、420 px 弹窗、父分类选择与 Enter 保存；再建同名提示错误，删除确认默认聚焦取消，取消保留分类，确认后移除。截图 `output/playwright/category-create.png` / `category-delete.png` 为未跟踪产物。
- 原生隔离包 `app.promptark.audit` 新建 `Category Audit` 后，新提示词继承该分类；删除确认后跳到未分类，`Keep category content` 正文保留，未分类从 1 变 2，软件开发从 1 变 0，主库总数仍为 2。仅新增/软删除隔离测试分类，未触碰正式分类和正文；测试包退出。
- 分类删除使用本机事务与软删除墓碑，JSON 导出排除已删分类，旧库初始化补充 `deleted_at`；同步回归为本机两库往返，不声称已执行真实云账号联调。
- 正式 macOS `.app` 构建通过，文档检查 192 份 Markdown 通过，差异检查通过；保留现有 Rust 未使用代码警告。未跟踪测试产物不提交。
