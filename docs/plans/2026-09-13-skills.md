# Skills 实施与验收记录

| 字段 | 值 |
|---|---|
| 状态 | 首版已实现；当前平台验收完成，跨客户端实际加载边界见下表 |
| 当前授权 | 2026-09-13 用户要求「根据 Skill 管理方案，实现一下 skill 管理，一次性完善好，并测试」 |
| 产品与行为方案 | [Skills 规格](../specs/skills/spec.md) |

**Goal:** 本轮完整交付方案中的首版范围。页面结构、客户端目录、安装策略与冲突行为以方案为唯一来源，不在本文重复。

**Architecture:** Vue 页面与现有主壳集成；Tauri/Rust 提供离线目录读取和有界文件操作；来源与安装记录独立于提示词内容。公开仓库下载不依赖现有提示词服务端。

## 当前进展

- [x] 查阅同类管理器、通用 CLI 和客户端官方文档。
- [x] 用户接受提示词/Skills 分组，各含广场和本地入口。
- [x] 汇总安装流程、维护流程、兼容边界与验收场景。
- [x] 方案阶段曾撤回提前新增的代码；本轮按新授权实施。
- [x] 用户已授权完整实施。

## 开发前准备

- [x] 已建立现行规格与 ADR 0025，更新 INDEX。
- [x] 复核官方目录约定与共享读取关系，记录各平台验收范围；未宣称各智能体已实际加载。
- [x] 核实公开来源接口、许可、限流与独立安装条件。
- [x] 确定来源、安装位置、操作记录的持久化结构及恢复机制。

## 切片 1：只读本机管理

**候选文件：** `desktop/src/components/SkillsPage.vue`、`desktop/src/platform/skills.js`、`desktop/src-tauri/src/skills/`、`desktop/src-tauri/src/commands/skills.rs`；最小调整 `WorkbenchShell.vue` 与 Tauri 命令注册。

- [x] 原生测试先覆盖目录展开、同名隔离、链接/缓存标注、有界扫描与错误。
- [x] 实现侧栏分组、本机列表、筛选、详情、目录管理；无网络依赖。
- [x] 验证编辑草稿保护、筛选恢复、空/错误/忙状态、键盘与主题。
- [x] 对应方案中的导航、扫描、聚合、外部管理、目录与平台边界场景。
- [x] 功能测试与文档门禁通过；基础实现提交 a689490，后续边界修复另行提交。

## 切片 2：本地安装与恢复

- [x] 测试完整复制、权限保留、多目标、范围选择、路径校验和失败恢复。
- [x] 实现安装预览、逐目标结果、来源/安装记录、移除与持久恢复。
- [x] 覆盖冲突、共享目录提示、重复点击和导航保护。
- [x] 对应方案中的完整安装、多目标、包校验、移除恢复与写入场景。
- [x] 功能测试与文档门禁通过；基础实现提交 a689490，后续边界修复另行提交。

## 切片 3：公开来源广场

- [x] 验证仓库地址解析、固定版本文件列表、限流/断网/截断和请求取消。
- [x] 接入候选仓库真实内容，完成搜索、来源与版本展示、详情及下载进度。
- [x] 用隔离临时目录完成真实仓库下载，不修改开发者正在使用的 Skill。
- [x] 对应方案中的真实广场和完整安装场景。
- [x] 功能测试与文档门禁通过；基础实现提交 a689490，后续边界修复另行提交。

## 切片 4：更新与发行验收

- [x] 验证远程更新、本地修改、差异预览、显式备份替换和失败回滚。
- [x] 运行适当前端/原生回归与生产构建。
- [x] 浅深色、窄窗口、长路径、多安装位置、键盘与辅助技术浏览器 QA。
- [x] 记录 macOS / Windows 五客户端的验收状态；实际加载未测组合明确标注，见下表。
- [x] 更新场景测试映射、平台边界与证据，随最终边界修复提交。

## 验证现状

本轮新增 23 项原生隔离测试、13 项组件/导航测试。全量前端 537 项通过；全量原生 112 项通过、5 项忽略（其中本轮 2 项真实网络测试已单独显式运行通过，另外 3 项为原有专用测试）。Vite 生产构建、Tauri debug 与 release 应用构建、文档门禁均通过。

真实公开来源验证：Anthropic 20 项、OpenAI Plugins 536 项、Vercel 9 项、Pi 8 项（2026-09-13 请求结果，不作为产品固定统计）。Anthropic algorithmic-art 的 4 个文件（59,784 字节）按提交 34040c9c568585f6929bedeaad110ad08f079624 完整下载并复制到 TempDir，逐文件校验通过。前三个仓库的 API 许可未明确返回，界面显示未知；Pi API 返回 MIT。

原生窗口验收使用独立标识 `app.cuetuck.skills-qa` 和 `/private/tmp/cuetuck-skills-acceptance/`：

- 通过系统目录选择器登记五个项目目录，确认登记后项目仍为空。
- 将一个包含 SKILL.md、脚本和参考资料的 3 文件测试包同时安装到五个目标，15 个文件 SHA-256 与原始文件一致，脚本执行权限保留；未执行脚本。
- 只移除共享目录内的一个测试副本，其余四个保留；退出客户端、重新启动，备份仍可见，恢复后文件校验一致。
- 实际只读扫描发现 344 个原有 Skill，缓存明确标注启用状态未知。原有 Skills 未被修改。
- 浅色列表、深色详情、窄内容区、长路径、共享目标确认、键盘焦点、辅助功能树均检查；组件回归覆盖草稿保护、筛选恢复、重复点击和写入导航保护。
- 构建产物为 `desktop/src-tauri/target/release/bundle/macos/CueTuck Skills QA.app`，未替换正式安装，也未发布新 GitHub Release。

### 平台边界

| 目标客户端 | macOS 标准项目目录完整安装 | 目标智能体实际加载 | Windows 实机 |
|---|---|---|---|
| Codex / 标准共享 | 已验证；另实测移除 / 重启恢复 | 未验证；不能推断其他共享读取客户端启用状态 | 未验证 |
| Claude Code | 已验证 | 未验证 | 未验证 |
| Cursor | 已验证 | 未验证 | 未验证 |
| Pi | 已验证 | 未验证 | 未验证 |
| OpenCode | 已验证 | 未验证 | 未验证 |

### 场景与测试映射

| 规格场景 | 验证位置 |
|---|---|
| 导航、草稿、筛选、忙状态、浏览器边界 | `SkillsPage.spec.js`、既有 WorkbenchShell 回归 |
| 同名、同源版本、物理别名 | `groupSkills` 组件测试；原生 `discovers_nested_same_names...` |
| 目录登记、环境变量、失效链接、系统缓存 | 原生 `supports_environment...`、`marks_links...`、`cannot_write_protected_descendants...` |
| 完整包、多目标、权限和部分失败 | 原生 `installs_complete_package...`、`multiple_targets...`、`preserves_executable...`；原生窗口五目标验收 |
| 冲突、修改、复制来源、差异确认 | 原生 `same_repo_version...`、`copying_a_managed_skill...`；组件确认和失败重试测试 |
| 更新、限流、取消、截断、简介错误 | 受控 HTTP `resolves_slash_branch...`、`remote_failures...`、`descriptions_are_real...` |
| 路径、特殊文件、限额、YAML 引用、暂存清理 | 原生 `rejects_unsafe_paths...`、`handles_encoded...`、`yaml_alias_expansion...`、`preview_cache_is_bounded...` |
| 崩溃恢复、移除后重启恢复、不覆盖 | 原生 `recovers_interrupted_rename...`、`rollback_keeps_original...`、`remove_restore_survives_restart...`；原生窗口验收 |

本机日志保存在 `/tmp/cuetuck-skills-full-ui-tests.log`、`/tmp/cuetuck-skills-full-native-tests.log`、`/tmp/cuetuck-skills-public-tests.log`、`/tmp/cuetuck-skills-release-build.log`。日志是本机临时验收证据，不进入发行包。

## 实施决策

- 索引、目录、来源、安装摘要和备份记录保存到应用数据目录 skills/registry.json，临时文件与原子替换写入；不进入提示词同步。
- 公开 GitHub 元数据用于解析分支与固定提交，文件从固定提交获取；当前来源搜索，GitHub 列表截断和限流明确报错。
- 安装先生成不可变暂存包，预览摘要/文件差异与目标状态，确认后逐目标落地；备份及待完成操作先持久化，恢复拒绝同名覆盖。
- 包限额 10 MiB/文件、25 MiB/包、1000 文件、16 层；扫描限制 20000 条目，超限作为可见警告。所有源码正文仅为展示数据，不执行。
- 测试以 TempDir 与可控网络替身覆盖失败路径，真实公开来源只写隔离目录；平台声明区分文件适配验证和客户端实际加载验证。
