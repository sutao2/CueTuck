# 功能复审与修复

| 字段 | 值 |
|---|---|
| 状态 | 现行，队首 |
| 关联 | [状态](status.md) · [分类规格](../specs/categories/spec.md) |

旧计划关闭不等于完整产品验收通过。按真实用户操作复查，每个切片通过回归后立即提交，未验收项不得标完成。

## 切片顺序

1. 本地分类：分类计数按内容、新建继承当前位置、保留未分类入口、筛选结果与标题一致。验证组件交互与 SQLite 筛选。
2. 广场分类：发布保存系统分类、列表过滤、下载保留分类及模型。验证 API → 桌面与 Web 下载，历史无分类条目仍可访问。
3. 同步完整性：自定义分类、合集与成员、模型、时间戳、删除状态的往返及冲突。验证独立设备、较新者胜、保留本地。
4. 工作台与数据操作：合集成员、复制失败、导入导出、设置刷新。逐个写失败场景再修复。
5. 其余模块复审：Web、管理台、认证、启动器、更新、账单；跑对应门禁，记录实际验证边界与剩余缺口。

## 当前验收

本轮实施顺序：先补桌面广场只读详情（加载、重试、关闭时丢弃过期响应、下载反馈），以组件交互和原生只读命令验证；随后补合集成员快照的发布、审核、下载事务，验证空合集与非法成员拒绝、成员分类模型保留及本地源不变。之后继续备份恢复与自动备份验收。

备份实施：SQLite 在线备份先写临时快照再替换目标；恢复先在临时目录迁移及校验，再用 SQLite 备份事务写入当前库，避免主文件替换与 WAL 冲突。ZIP 从同一快照导出 SQLite 与 JSON。原生后台每分钟检查开关、距上次成功 24 小时后备份；失败可见、后续重试。验证 WAL 最新提交、损坏/旧格式库、写锁失败保持原库、自动备份到期与关闭行为。

管理台实施：审核状态和上架记录在同一事务提交；同结果重试不重复上架，不同结果冲突返回 409；组件禁用进行中操作，失败保留条目并允许重试。验证并发审核、模拟上架失败回滚和无快照不误上架。

Web 实施：登录入口与退出、访客/账号内存隔离、旧账号读取响应丢弃；保存失败保留待同步副本并可重试，重新读取不覆盖待同步编辑；复制失败保留变量与预览并可重试。验证跨账号响应、失败保存重试和剪贴板拒绝，不宣称标签页数据已永久持久化。

- [x] 切片 1：桌面 172 个测试通过，SQLite 未分类回归通过，docs-check 180 文件通过。
- [x] 切片 2（单条提示词）：分类发布审核下载与模型保留已接通；桌面 176、Web 20、后端内存 30 测试通过，桌面/Web 构建通过。合集发布快照纳入切片 4。
- [x] 切片 3：完整快照按依赖顺序事务落地，保留模型/成员/软删除；统一时间比较、Web 编辑保留元数据、Wi-Fi 恢复补齐封面。桌面 181、Web 21、Rust 桌面 47（另 1 性能项忽略）、后端非 Postgres 42 项通过；构建与 docs-check 通过。Postgres 集成限制仍见下文。
- [x] 切片 4（已列功能切片通过；封面外部资源与剩余跨平台验收见下文）
  - [x] 合集维护与复制：全库选择成员、打开/使用/移除成员、编辑/软删除合集；复制失败保留弹窗且不计数。桌面 184、Rust 桌面 48 项通过（另 1 性能项忽略）；浏览器实测新建与分类保存、跨分类加入成员及变量预览，截图人工检查通过。
  - [x] JSON 数据操作：保留分类/合集/成员/模型/封面的副本导入，兼容旧格式，事务拒绝坏引用与非法字段；预览变更失效、成功后防重复提交、刷新分类树和偏好。桌面 189、Rust 桌面 49 项通过（另 1 性能项忽略），构建和文档门禁通过。
  - [x] 离线队列：成功的新操作清除旧同源任务，串行处理防并发丢失，换账号停止重放；界面区分入队与送达。桌面 193 项通过，构建和文档门禁通过。
  - [x] 桌面广场详情：只读正文、失败重试、关闭后丢弃迟到响应、下载状态反馈；桌面 196、Rust 桌面 49 项通过（另 1 性能项忽略），构建与文档门禁通过。
  - [x] 合集发布快照：提交当前成员、离线保留类型与快照、审核正文预览、保持合集类型上架、桌面事务下载/Web 成员归属。桌面 200、Web 23、管理台 8、Rust 桌面 49（另 1 性能项忽略）、后端 50 项通过（含 6 个 Postgres 测试）；三端构建与文档门禁通过。真实浏览器使用隔离 API 数据验证详情、下载及本地成员展示。
  - [x] 备份恢复/自动备份：在线一致性快照、临时迁移校验、恢复失败不写半库、ZIP 同源、原生每分钟检查/24 小时备份、可见失败与浏览器拒绝假开关。桌面 202、Rust 桌面 55 项通过（另 1 性能项忽略），构建与文档门禁通过；时间调度用注入时间验证，未声称已连续运行 24 小时。
- [ ] 切片 5
  - [x] 管理台审核：状态与上架事务提交、同结果幂等/相反结果 409、并发与故障回滚、按钮忙状态及失败重试。后端 52 项通过（含 7 个 Postgres 测试），管理台 9 项与构建通过。
  - [x] Web 账号与失败反馈：登录/退出入口、访客与账号隔离、取消登录后忽略响应、待同步编辑保留及重试、读取错误可见、剪贴板失败可重试。Web 27 项与构建通过。

生产托管、商店账号、Windows/Linux 设备验证仍见 [deferred](deferred.md)。不修改真实用户提示词的分类或正文来掩盖产品问题。

## 验证限制

- 本机 Docker Postgres 已恢复；原 5 个持久化测试及新增合集重启回归通过，字段迁移已在随机隔离 schema 验证。生产环境与平台发行仍需独立验收。
- 原生 Tauri 默认 camelCase，旧客户端传 snake_case，造成分类丢失/鉴权失败。已用共享桥接修复六个模块并增加原生命令参数测试。

## 后续优先复现项（未修复）

- 合集发布与详情已接通；本切片不上传本机封面资源，历史无成员的占位合集明确不可下载。
- 审核重复提交及事务缺口已修复；无正文快照的旧投稿不会被误上架。
- 备份恢复已验证 WAL、旧库兼容、非法结构与写锁回滚；恢复系统快捷键/代理等需重启，UI 已提示。ZIP 保留封面引用，不打包外部图片文件。
- Web、管理台、登录切换、启动器和更新/账单仍需逐项运行完整操作验收，不能用现有单元测试数量代表全部功能完成。
