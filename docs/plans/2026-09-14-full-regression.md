# 全量回归与交互边界修复

状态：已完成，本机修复版已安装并实机验收。

1. 执行三前端测试与构建、原生和 MCP 全量测试；服务端使用远程 CI 隔离数据库，记录本机依赖缺失与失败，不混作通过。
2. 浏览器回归覆盖创建/编辑/变量/复制、图片下载补全与预览、分类整理、发布、设置和后台审核失败重试；原生检查广场、Skill、更新和重启登录。
3. 对已确认问题先补失败用例再修复：启动器布局变化意外触发悬停选中；下拉框右侧溢出、百项以外的当前值被回车改掉。登录恢复测试使用无代理的本地假服务，避免环境代理干扰。
4. 补验、构建并安装修复版；提交规格和验收记录。当前任务不另建公开发行版。

- Given 启动器列表展开且鼠标静止 When 新行出现在鼠标下 Then 不改变键盘高亮；实际移动鼠标后仍可选行。
- macOS 实机补验发现布局变动也会产生零位移 mousemove，选行须检查实际位移，不能仅把 mouseenter 换成 mousemove。
- Given 下拉框靠近窗口右边且宽度大于 280px When 打开 Then 弹出层保持在视口内。
- Given 当前模型位于第 100 项之后 When 打开并直接回车 Then 保留当前模型，渲染不超过 100 项，搜索仍可找到全部选项。
- Given 本机配置网络代理 When 登录恢复测试请求隔离 loopback 服务 Then 测试直接连接假服务，不依赖外部代理。

## 结果与覆盖范围

- 三处交互问题已补出失败用例并修复：启动器静止鼠标误选操作、百项外模型回车误改、宽下拉弹层越过右边界。实现为 `e0ddef8`，macOS 零位移事件补充为 `9030248`。
- 本机桌面 586、Web 33、管理端 104、原生 127、MCP 27 项测试通过；脚本测试 12 项通过、1 项跳过。最后的零位移补充又通过启动器相关 40 项测试。
- 三前端构建、304 份 Markdown 校验通过；最终生产地址 macOS app 构建并严格签名检查通过，已安装到 `/Applications/CueTuck.app`。
- 桌面隔离浏览器 smoke 覆盖创建/编辑/变量/复制/删除取消、图片下载补全、预览缩放、窄窗口、分类整理、合集、文件导入、发布、明暗色和返回位置；管理端 smoke 覆盖登录、审核、列表恢复、保存失败重试、退出及 1440/800/390 宽度。用户占用 1432，管理 smoke 用临时脚本改为 1442，已清理。
- 原生实机确认：344 个本机 Skill 扫描与详情读取；广场软件开发分类显示 524 条且侧栏全量仍为 22392；详情展示中文缓存译文与来源作者；检查更新能完成并显示发行说明。
- 最终包实机执行启动器输入“吉他” → 首条保持高亮 → Return 打开变量 → Return 复制 → Cmd+V 粘贴出完整中文。退出再打开恢复账号，本地 11 条提示词保留；测试草稿未保存，搜索已清空。
- 本机 PostgreSQL 未启动，首次后端测试因依赖不可用中止，未记为通过；相同服务端源码在 [全量 CI 34849173622](https://github.com/sutao2/CueTuck/actions/runs/34849173622) 的隔离 PostgreSQL 下通过，三项 CI job 均成功。原生首次全量的两个恢复测试波动，独立及全量复跑通过，并明确隔离测试代理。
- 本次不重复真实 OAuth 授权、不发邮件或执行付费操作；发布/下载写入使用隔离浏览器数据，Skill 安装/恢复依赖原生临时目录测试；Windows 未做人工桌面操作。检查结果不等于不存在其他缺陷。没有创建新的公开发行版本。
