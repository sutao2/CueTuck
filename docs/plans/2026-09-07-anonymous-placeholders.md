# 空占位符填写

状态：已完成。截图正文使用三个 `{}`，旧实现只识别非空 `{{名称}}`，因此误入无变量预览。仅修复桌面共享解析器及对应入口提示，不修改用户保存的正文，不扩展到单花括号命名语法或 Web 独立实现。

## 实施与验收

1. 用截图正文添加失败回归：应出现三个独立空位，依次填写后预览/回车复制顺序正确；主窗口向导同样可填写。
2. 非代码区域的 `{}` / `{ }` 按出现顺序分配独立「占位符 N」，与已有同名变量避撞。未填保留原文；`{{名称}}` 保留同名共享语义，空双括号仍忽略。
3. 匿名空位不匹配代码围栏、行内代码、引号字符串、嵌套对象内及反斜杠转义的花括号。不猜测没有代码标记的独立 `{}` 是对象还是空位；需字面花括号时用行内代码/围栏/转义，或留空保持原文。
4. 解析与主窗口/启动器回归，真实浏览器用截图样例逐项 Enter 验证预览与复制；前端构建、文档检查后提交，并更新本地 macOS 包。

## 验收记录

- 修复前截图、混合命名、代码边界和启动器四项新增回归均失败，修复后通过；补充主窗口向导与中间漏填回归。
- 桌面前端 37 个文件、269 项测试通过，生产前端构建通过。
- Playwright 真实浏览器中用截图正文验证三个字段，实际键盘 Enter 按 1 → 2 → 3 推进，最后 Enter 触发一次复制。预览与浏览器剪贴板桩均为 `Sql A dejk fer B. hdjjf dev C jhdfhk sd`。截图：`output/playwright/anonymous-placeholder-fill.png`（本地验收产物，不提交）。本次未重新验证系统剪贴板或跨应用粘贴权限路径。
- 本地 macOS debug `.app` 打包成功（仅本次关闭更新签名产物，保留仓库签名配置）；运行中的 Tauri 开发进程可接收前端热更新。未修改原提示词数据。
- `scripts/docs-check`（187 份 Markdown）与 `git diff --check` 通过；按已授权的临时 WorkLog 门禁豁免提交，不修改永久钩子配置。
