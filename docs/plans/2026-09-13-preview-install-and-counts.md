# 预览安装与下载计数修复

- 日期：2026-09-13
- 状态：Mac 修复版与 API 已发布；GUI 安装启动已验证，Windows 构建调度受阻
- 授权：用户要求修复 macOS 已损坏提示，并选择成功下载默认计数、隐私设置可关闭。

## 实施顺序

1. 核验已安装应用的签名、版本及与发布构建的可执行文件摘要；仅对确认可信的 PromptArk 移除下载隔离标记，验证启动。发布页补充无证书安装指引，不关闭全局 Gatekeeper，不伪称已公证。
2. 未保存过匿名统计偏好时默认开启，保留显式关闭。成功保存完整内容后异步上报；服务端原子递增并返回真实次数，客户端更新对应卡片，不等待统计才确认本地下载成功。
3. 验证默认/关闭、失败、重复下载、并发计数、延迟返回与页面卸载；更新规格、提交，部署后端并构建下一预览版。

## Given / When / Then

- GIVEN 下载的 macOS App 完整且与可信发行一致，只有隔离限制；WHEN 仅放行该应用；THEN 可以打开且原有签名仍通过，不改变全局安全设置。真正损坏的签名不得用重新签名掩盖。
- GIVEN 尚未设置统计偏好；WHEN 成功保存正文及全部附件；THEN 默认匿名上报一次并在收到服务端计数后更新卡片。
- GIVEN 用户明确关闭统计；WHEN 更新客户端或下载；THEN 保留关闭，不发统计请求。
- GIVEN 下载失败、并发点击或打开已有本地副本；WHEN 操作结束；THEN 不增加失败/重复下载次数。
- GIVEN 统计失败、超时或返回无计数的旧接口；WHEN 下载已保存；THEN 本地成功不被撤销，不伪造次数；迟到响应不得更新已卸载组件。

## 验收

- 504 项前端、85 项 macOS 原生（2 项既有忽略测试）、4 项后端计数/并发/热门测试、1 项真实 Postgres 持久化及 5 项发行检查通过。
- 安装的 beta.2 签名严格校验通过，可执行文件 SHA-256 与可信发布构建一致；Chrome 隔离标记存在且 Gatekeeper 拒绝。仅递归清除此应用的隔离属性，复验签名通过；未修改全局 Gatekeeper。初次启动验证被自动审批拒绝；用户随后明确授权安装并启动，验证结果见下。未声称已解决所有 Mac 的首次拦截。
- beta.2 发布说明已补上安装核验/首次放行指引；现行步骤见[部署说明](../../deploy/README.md)。
- v0.1.0-beta.3 Mac arm64 已公开，源标签 `5238a2b392167898efc268c98e9a33615cfdf8ca`；DMG 10,239,007 字节，SHA-256 `52fa000cafd194905497131d70bb631b804deb92c8da81eff672eb64c59e9381`。app 签名、DMG 完整性和 GitHub 资产摘要通过；不提供自动更新清单或伪称 Developer ID/公证。
- Windows 工作流三次调度均 HTTP 500，未创建 beta.3 任务，未上传旧 exe 冒充新版本；beta.2 历史安装包保留。
- 线上 API 计数修复已部署并 healthy，`/v1/health` 三项 true，热门分页两项数值有效、总数仍 22,391。管理端、私有服务和卷保持原实例。生产计数写入冒烟测试被自动审批拒绝，未制造生产下载数据；准确性由本地真实数据库与并发测试验证。

- 用户明确授权后，已备份旧应用并安装 `/Applications/PromptArk.app`，严格签名验证通过。真实 GUI 更新页显示 `0.1.0-beta.3`，启动无已损坏提示，隐私页匿名统计开关为开启，广场成功加载 22,391 条。原有 6 条本地提示词及附件仍显示；当前会话显示未登录，需用户重新登录。未执行会增加生产计数的测试下载。旧应用已在本机备份。
