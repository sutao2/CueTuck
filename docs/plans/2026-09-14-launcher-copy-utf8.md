# macOS 启动器中文复制

状态：已完成，本机安装验收通过。

1. 原生实机复现：短英文 `javs` 回车后可粘贴；中文“吉他品丝”参数确认后窗口关闭，粘贴为空。现有 pbcopy 继承应用 locale，仅检查退出码；macOS 本机 pbcopy 手册规定缺少编码时退回 C 编码。
2. 为剪贴板子进程明确设置 UTF-8，写入后读取比对；不匹配返回错误，沿用失败保留预览、不记使用、不隐藏窗口的流程。
3. 测试子进程编码配置、Unicode 回读一致性和复制失败不关闭；构建安装后实机复验中文、英文与多行内容，再提交。

- Given GUI 启动环境没有 UTF-8 locale When 复制中文或 emoji Then 子进程明确使用 UTF-8，回读一致才成功。
- Given 子进程退出成功但内容为空或不一致 When 完成复制 Then 报错且保留预览，不关闭、不记使用。
- Given 回车复制中文参数结果 When 窗口关闭后粘贴 Then 与预览正文一致。

## 验收记录

- 启动器相关前端测试 51 项、原生编码与回读测试 2 项通过；文档校验通过。
- 生产 API 地址构建成功，应用通过严格签名检查，已安装到 `/Applications/CueTuck.app`。
- 实机重走“吉他品丝”变量预览 → Return → 窗口关闭 → Cmd+V，中文全文与预览一致。再以中文、`🦜`、换行和 `café` 混合变量复制，粘贴到多行输入框确认全部保留。
- 验证后取消临时变量草稿并清空搜索；本地 11 条提示词与账号登录状态保留。
- 实现提交为 `03d8560`；本次安装为本机构建，公开的 beta.13 安装包尚未包含此修复。
